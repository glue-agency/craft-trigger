<?php

namespace GlueAgency\Trigger\Controllers;

use Craft;
use craft\base\Plugin;
use craft\web\Controller;

class TriggerController extends Controller
{

    public function actionIndex()
    {
//        $label = \GlueAgency\Trigger\Plugin::getInstance()->settings->label;
//        $url = \GlueAgency\Trigger\Plugin::getInstance()->settings->url;
//
//        Craft::$app->session->setFlash('notice',"Trigger action complete: ".$url);
//
//        $this->redirect(Craft::$app->getRequest()->referrer);
    }
}